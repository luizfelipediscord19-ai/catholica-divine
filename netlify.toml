[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Nitro's "netlify" preset emits static files in dist and a Netlify Function at
# .netlify/functions-internal/server. No extra redirects needed.

[functions]
  node_bundler = "esbuild"
